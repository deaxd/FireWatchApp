<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.hfad.statistics">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">

        <activity android:name=".StatisticsActivity"
            android:label="Statistika"
            android:parentActivityName="foi.hr.firewatchapp.MainActivity"
            android:screenOrientation="portrait"/>

        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="foi.hr.firewatchapp.MainActivity" />


    </application>

</manifest>
